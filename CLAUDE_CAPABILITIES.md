# מה אפשר לעשות עם Claude Code בפרויקט Tango

מסמך זה מציג את הדברים המעניינים והשימושיים ביותר שניתן לעשות עם Claude Code, בהתאמה לפרויקט Tango — ויזואליזציה של צינור משימות בתהליכי רישוי חקלאי/בנייה.

---

## 1. פירוק קוד גדול לרכיבים מסודרים

`App.jsx` הנוכחי הוא 718 שורות — נתונים, לוגיקה ורכיבים בקובץ אחד.

**מה אפשר לבקש:**
```
פצל את App.jsx לרכיבים נפרדים: PhaseCard, TaskRow, Pill, DependencyPanel.
העבר את RAW_DATA לקובץ data/tasks.js נפרד.
```

**למה זה מעניין:** Claude Code קורא את כל הקוד, מבין את הקשרים בין הרכיבים, ומבצע את הפירוק בלי לשבור שום דבר.

---

## 2. הוספת מערכת בדיקות מאפס

הפרויקט אין לו ולו בדיקה אחת. לוגיקת התלויות בין 167+ המשימות היא מורכבת ועלולה להישבר.

**מה אפשר לבקש:**
```
הוסף Vitest לפרויקט.
כתוב בדיקות ליוניט לפונקציות resolving התלויות.
כתוב בדיקת אינטגרציה שמוודאת שכל משימה עם תלויות — התלויות שלה קיימות ב-RAW_DATA.
```

**למה זה מעניין:** Claude Code יזהה את הפונקציות הקריטיות, יכתוב מקרי קצה, ויגלה באגים סמויים בנתונים.

---

## 3. זיהוי בעיות בנתונים

167 משימות, עם תלויות מפורשות — סביר שיש טעויות נסתרות (תלות על משימה שלא קיימת, מחזוריות, קטגוריה שגויה).

**מה אפשר לבקש:**
```
סרוק את RAW_DATA ומצא:
- תלויות שמפנות ל-id שלא קיים
- משימות ללא id ייחודי
- מחזורים (circular dependencies) בגרף התלויות
```

**למה זה מעניין:** Claude Code יכתוב סקריפט ניתוח חד-פעמי, ירוץ עליו, ויציג דוח בעיות.

---

## 4. הוספת חיפוש וסינון חי

כרגע אין דרך לחפש משימה לפי שם, לסנן לפי סטטוס, או לזהות איפה נמצאת משימה ספציפית בצינור.

**מה אפשר לבקש:**
```
הוסף שורת חיפוש שמסננת משימות בזמן הקלדה.
הוסף פילטר לפי שלב (phase) ולפי סטטוס תלויות.
הדגש את תוצאות החיפוש עם highlight.
```

**למה זה מעניין:** Claude Code יממש state management, debounce, והדגשת תוצאות — כל זה בהתאם למבנה הקיים.

---

## 5. יצוא ויבוא נתונים

הנתונים כרגע hardcoded. זה מקשה על עדכון המשימות.

**מה אפשר לבקש:**
```
הוסף כפתור "ייצא JSON" שמוריד את ה-RAW_DATA כקובץ.
הוסף כפתור "טען קובץ" שמאפשר לטעון נתונים מחדש מ-JSON.
```

**למה זה מעניין:** זה הופך את האפליקציה לכלי שניתן לעדכן בלי לגעת בקוד.

---

## 6. תצוגת Gantt Chart

הצינור הנוכחי הוא ויזואלי אבל לא מראה ציר זמן. אם יש משכי זמן למשימות — ניתן להוסיף תצוגת גנט.

**מה אפשר לבקש:**
```
הוסף שדה duration לכל משימה ב-RAW_DATA.
בנה תצוגת Gantt Chart אופקית לפי שלבים, עם ציר זמן.
אפשר toggle בין תצוגת הפייפליין הנוכחית לגנט.
```

**למה זה מעניין:** Claude Code יתכנן את המבנה, יבחר ספרייה מתאימה (כמו `react-gantt-chart`), ויממש אותה.

---

## 7. CI/CD עם GitHub Actions

אין כרגע שום אוטומציה — כל push עלול לשבור את ה-build בלי שמישהו ישים לב.

**מה אפשר לבקש:**
```
צור GitHub Actions workflow שמריץ lint ו-build על כל push ו-PR.
הוסף badge ל-README שמציג סטטוס ה-build.
```

**למה זה מעניין:** Claude Code יכיר את מבנה הפרויקט ויוצר `.github/workflows/ci.yml` מתאים.

---

## 8. מיגרציה ל-TypeScript

הפרויקט ב-JavaScript. עם 167+ פריטי נתונים ומבנים מורכבים, TypeScript ימנע bugs בזמן כתיבה.

**מה אפשר לבקש:**
```
העבר את הפרויקט ל-TypeScript.
הגדר טיפוסים ל-Task, Phase, Dependency.
```

**למה זה מעניין:** Claude Code יגדיר ממשקים מתוך ה-RAW_DATA הקיים, יהפוך את הקבצים ל-.tsx, ויתקן שגיאות טיפוסים.

---

## 9. שיפור ביצועים

עם 167 משימות ואפשרות לגדול, רינדור לא מאוחסן יכול להאט את האפליקציה.

**מה אפשר לבקש:**
```
הוסף React.memo ו-useMemo לרכיבים שמחושבים הרבה.
הוסף וירטואליזציה לרשימת המשימות הארוכה.
פרופל את האפליקציה ומצא צווארי בקבוק.
```

---

## 10. תיעוד אוטומטי

**מה אפשר לבקש:**
```
קרא את כל הקוד וכתוב README מפורט לפרויקט:
- מה המוצר עושה
- איך להריץ מקומית
- מבנה הנתונים של RAW_DATA
- איך להוסיף שלב או משימה חדשה
```

**למה זה מעניין:** Claude Code יקרא את הקוד בפועל ויכתוב תיעוד מדויק — לא תבנית גנרית.

---

## טיפים לעבודה עם Claude Code על Tango

| בקשה | טיפ |
|------|-----|
| שינוי גדול בנתונים | בקש קודם "קרא את RAW_DATA והסבר את המבנה" |
| תוספת feature | בקש "אל תשנה דבר שלא קשור לפיצ'ר" |
| debug | ספק את הודעת השגיאה המלאה + שם הקובץ |
| refactoring | בקש תמיד "תכתוב קודם תוכנית, אל תתחיל לממש" |
| שינויים גדולים | השתמש ב-Plan Mode (`Shift+Tab` פעמיים) |

---

> נכתב על ידי Claude Code — 2026-03-29
